<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>User Dashboard - Lyra Beauty</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/style.css">
</head>
<body class="gold-bg">
  <div class="container">
    <h2>User Dashboard</h2>
    <p>Welcome, <%= user.display_name || user.first_name || user.username %>!</p>
    
    <% if (user.role === 'partner') { %>
      <div class="dashboard-links mb-3">
        <a href="/partner-dashboard" class="btn btn-primary mb-2">Partner Dashboard</a>
        <a href="/referrals" class="btn btn-primary mb-2">Referrals</a>
        <a href="/commissions" class="btn btn-primary mb-2">Commissions</a>
      </div>
    <% } else if (user.role === 'customer' || user.role === 'user') { %>
      <div class="dashboard-links mb-3">
        <a href="/book" class="btn btn-primary mb-2">Book Appointment</a>
        <a href="/my-appointments" class="btn btn-primary mb-2">My Appointments</a>
      </div>
    <% } else { %>
      <div class="dashboard-links mb-3">
        <a href="/book" class="btn btn-primary mb-2">Book Appointment</a>
        <a href="/my-appointments" class="btn btn-primary mb-2">My Appointments</a>
      </div>
    <% } %>
    <a href="/logout" class="btn btn-secondary">Logout</a>
  </div>
</body>
</html>
