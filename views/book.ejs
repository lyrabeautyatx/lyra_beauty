<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Book Appointment - Lyra Beauty</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/style.css">
</head>
<body class="gold-bg">
  <div class="container">
    <h2>Book an Appointment</h2>
    
    <% if (typeof error !== 'undefined' && error) { %>
      <p class="error"><%= error %></p>
    <% } %>
    
    <form method="GET" action="/book" class="date-form">
      <label for="date">Select Date:</label>
      <input type="date" name="date" value="<%= selectedDate %>" min="<%= moment().format('YYYY-MM-DD') %>" onchange="this.form.submit()">
    </form>
    
    <% if (availableSlots.length > 0) { %>
      <form method="POST" action="/book">
        <input type="hidden" name="date" value="<%= selectedDate %>">
        
        <div class="form-group">
          <label for="service">Service:</label>
          <select name="service" required>
            <option value="microblading" <%= preSelectedService === 'microblading' ? 'selected' : '' %>>Microblading - $<%= (servicePricing.microblading.price / 100).toFixed(2) %></option>
            <option value="microshading" <%= preSelectedService === 'microshading' ? 'selected' : '' %>>Microshading - $<%= (servicePricing.microshading.price / 100).toFixed(2) %></option>
            <option value="lipglow" <%= preSelectedService === 'lipglow' ? 'selected' : '' %>>Lip Glow - $<%= (servicePricing.lipglow.price / 100).toFixed(2) %></option>
            <option value="browmapping" <%= preSelectedService === 'browmapping' ? 'selected' : '' %>>Brow Mapping - $<%= (servicePricing.browmapping.price / 100).toFixed(2) %></option>
          </select>
        </div>
        
        <div class="form-group">
          <label>Available Time Slots:</label>
          <div class="time-slots">
            <% availableSlots.forEach(slot => { %>
              <label class="time-slot">
                <input type="radio" name="time" value="<%= slot %>" required>
                <%= slot %>
              </label>
            <% }); %>
          </div>
        </div>

        <div class="form-group">
          <label for="couponCode">Coupon Code (Optional):</label>
          <input type="text" name="couponCode" id="couponCode" placeholder="Enter coupon code (e.g., penguin10off)" maxlength="50" style="text-transform: lowercase;">
          <small class="form-text text-muted">
            Have a referral code? Enter it here for a 10% discount! Each customer can only use one coupon ever.
          </small>
        </div>
        
  <button type="submit" class="btn btn-primary">Proceed to Payment</button>
      </form>
    <% } else { %>
      <p>No available slots for this date. Please select another date.</p>
    <% } %>
    
  <a href="/dashboard" class="btn btn-secondary">Back to Dashboard</a>
  </div>
</body>
</html>
