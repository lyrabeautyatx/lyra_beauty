<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Application Status - Lyra Beauty</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/style.css">
</head>
<body class="gold-bg">
  <div class="container mt-4">
    <div class="row">
      <div class="col-12">
        <div class="d-flex justify-content-between align-items-center mb-4">
          <h2>Partner Application Status</h2>
          <a href="/dashboard" class="btn btn-outline-primary">Back to Dashboard</a>
        </div>

        <% if (applications.length === 0) { %>
          <div class="card">
            <div class="card-body text-center py-5">
              <h5>No Applications Found</h5>
              <p class="text-muted">You haven't submitted any partner applications yet.</p>
              <a href="/apply-partner" class="btn btn-primary">Apply for Partnership</a>
            </div>
          </div>
        <% } else { %>
          <% applications.forEach(application => { %>
            <div class="card mb-3">
              <div class="card-header d-flex justify-content-between align-items-center">
                <h5 class="mb-0">Application #<%= application.id %></h5>
                <span class="badge <%= application.status === 'approved' ? 'bg-success' : 
                                       application.status === 'rejected' ? 'bg-danger' : 'bg-warning' %>">
                  <%= application.status.toUpperCase() %>
                </span>
              </div>
              <div class="card-body">
                <div class="row">
                  <div class="col-md-6">
                    <strong>Business Name:</strong><br>
                    <%= application.business_name %>
                  </div>
                  <div class="col-md-6">
                    <strong>Submitted:</strong><br>
                    <%= new Date(application.created_at).toLocaleDateString() %>
                  </div>
                </div>

                <% if (application.business_description) { %>
                  <div class="mt-3">
                    <strong>Business Description:</strong><br>
                    <div class="text-muted"><%= application.business_description %></div>
                  </div>
                <% } %>

                <div class="mt-3">
                  <strong>Why Partner:</strong><br>
                  <div class="text-muted"><%= application.why_partner %></div>
                </div>

                <% if (application.status !== 'pending') { %>
                  <hr>
                  <div class="row">
                    <div class="col-md-6">
                      <strong>Reviewed:</strong><br>
                      <%= application.reviewed_at ? new Date(application.reviewed_at).toLocaleDateString() : 'N/A' %>
                    </div>
                    <div class="col-md-6">
                      <strong>Status:</strong><br>
                      <span class="badge <%= application.status === 'approved' ? 'bg-success' : 'bg-danger' %>">
                        <%= application.status === 'approved' ? 'APPROVED' : 'REJECTED' %>
                      </span>
                    </div>
                  </div>

                  <% if (application.admin_notes) { %>
                    <div class="mt-3">
                      <strong>Admin Notes:</strong><br>
                      <div class="alert alert-info">
                        <%= application.admin_notes %>
                      </div>
                    </div>
                  <% } %>

                  <% if (application.status === 'approved') { %>
                    <div class="alert alert-success mt-3">
                      <h6>🎉 Congratulations!</h6>
                      <p class="mb-0">Your partnership application has been approved! You now have access to the partner dashboard and can start earning commissions on referrals.</p>
                    </div>
                  <% } else if (application.status === 'rejected') { %>
                    <div class="alert alert-warning mt-3">
                      <h6>Application Not Approved</h6>
                      <p class="mb-0">Unfortunately, your partnership application was not approved at this time. You may submit a new application in the future.</p>
                    </div>
                  <% } %>
                <% } else { %>
                  <hr>
                  <div class="alert alert-info">
                    <h6>📋 Application Under Review</h6>
                    <p class="mb-0">Your application is currently being reviewed by our admin team. You will receive an email notification when a decision is made.</p>
                  </div>
                <% } %>
              </div>
            </div>
          <% }) %>
        <% } %>

        <% if (user.role === 'customer' && applications.length === 0) { %>
          <div class="text-center mt-4">
            <p class="text-muted">Ready to start earning commissions?</p>
            <a href="/apply-partner" class="btn btn-primary btn-lg">Apply for Partnership</a>
          </div>
        <% } %>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>